#!/bin/bash

PACKAGE=$1

if [[ $# -le 0 ]]; then 
    echo "No arguments given"
    exit 1
fi

if [[ "$@*" == *"-y"* ]]; then 
    AUTO_Y="-y"
fi

echo $AUTO_Y

if sudo apt install $PACKAGE $AUTO_Y; then 
    echo "$PACKAGE" >> ~/.saved-packages
else
    echo "$PACKAGE not saved."
fi