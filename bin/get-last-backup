#!/bin/bash

clean=$(find $BACKUP_DIR -maxdepth 2 -mmin -66 | grep @ | head -1)

clean=${clean#*.}

DAY=${clean:6:2}
MONTH=${clean:4:2}
HOUR=${clean:9:2}
MIN=${clean:11:2}
echo $DAY/$MONTH $HOUR:$MIN


